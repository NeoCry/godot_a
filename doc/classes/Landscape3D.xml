<?xml version="1.0" encoding="UTF-8" ?>
<class name="Landscape3D" inherits="Node3D" api_type="core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../class.xsd">
	<brief_description>
		A large, sculptable, texture-splatted heightmap terrain, in the spirit of CryEngine's terrain system.
	</brief_description>
	<description>
		[Landscape3D] renders a [TerrainData] heightmap ([member terrain_data]) as a grid of small chunks, each with its own automatic level of detail: nearby chunks render at full density while distant ones use a cheaper, decimated mesh with no extra scripting or per-frame cost, using the same mesh LOD mechanism as [method ArrayMesh.add_surface_from_arrays]'s [code]lods[/code] parameter. A thin "skirt" of hidden geometry around each chunk's edges hides the small seams that can appear where two neighboring chunks are rendered at different levels of detail.
		Up to 32 [TerrainLayer]s ([member layers]) can be painted onto the terrain. Every point on the terrain has its own independent weight for every layer (see [TerrainData]'s description); what's actually rendered there is every layer's material mixed in proportion to its share of the total weight, the same "weight-blended" model CryEngine and UE4/5's Landscape layers use, so any number of layers can overlap smoothly at once (not just two at a time) and painting one layer smoothly displaces the others rather than producing hard edges at the vertex grid. A [member Landscape3D.collision_layer] configurable [HeightMapShape3D] is kept in sync automatically so physics bodies collide with the terrain; see [method update_collision].
		While selected in the editor, a toolbar appears in the 3D viewport with a brush for sculpting (raising, lowering, smoothing, and flattening the heightmap), painting texture layers, and cutting or filling holes (openings in the terrain's geometry itself, e.g. for a cave entrance, that the camera and physics bodies can pass through - see [method TerrainData.is_hole]). The same operations are available from a script through [method sculpt], [method paint_layer], and [method set_hole], for example to carve a crater into the terrain at runtime. The toolbar's [b]Import Heightmap...[/b] button imports a square grayscale image as the whole heightmap (see [method TerrainData.import_heightmap] for format/precision recommendations - EXR or HDR, not PNG or a lossy format), creating a [TerrainData] automatically if [member terrain_data] isn't set yet; this replaces the current heightmap and is not undoable.
		[FoliagePainter3D] and [FoliageSpawner3D] both recognize a [Landscape3D] as a paintable/projectable ground surface alongside [MeshInstance3D], so vegetation can be scattered directly onto it.
		The terrain also acts as an occluder ([member occluder_enabled]): once occlusion culling is enabled (see [member ProjectSettings.rendering/occlusion_culling/use_occlusion_culling], or [member Viewport.use_occlusion_culling] for a custom viewport), whatever stands behind a hill or a ridge is skipped entirely instead of being drawn and then hidden by the terrain. This is what keeps a densely vegetated terrain affordable, since the [FoliagePainter3D]/[FoliageSpawner3D] cells behind a slope are usually most of the vegetation in view. No baking or extra nodes are involved: each chunk hands the renderer a simplified copy of its own surface, kept up to date as the terrain is sculpted.
		[member pom_enabled] is a master switch for Parallax Occlusion Mapping: it must be [code]true[/code] here [i]and[/i] on each individual [TerrainLayer] ([member TerrainLayer.pom_enabled]) for that layer to use it, so a layer with no real height detail never pays for the ray-marching this adds. An enabled layer's [member TerrainLayer.height_texture] gives it extra apparent surface depth (bumps, grooves) from the shader offsetting its texture lookups, without adding any actual geometry. [member pom_self_shadow_enabled] additionally darkens crevices that would be shadowed by nearby raised detail, approximated using the fixed [member pom_shadow_light_direction] rather than any actual light in the scene (a plain shader can't see scene lights the way a light-processor function can). Independently, [member TerrainLayer.triplanar_enabled] projects a layer's textures from world-space planes instead of a single UV, useful for steep terrain (cliffs) where ordinary UVs would stretch.
		[b]Note:[/b] [Landscape3D] is intended to be used unrotated (only its position and scale matter for mapping world space to the weight maps and to texture tiling); rotating it is not supported.
	</description>
	<tutorials>
	</tutorials>
	<methods>
		<method name="get_aabb" qualifiers="const">
			<return type="AABB" />
			<description>
				Returns a local-space bounding box covering the terrain's full footprint, padded generously above and below since sculpted height has no fixed range.
			</description>
		</method>
		<method name="get_height_region" qualifiers="const">
			<return type="PackedFloat32Array" />
			<param index="0" name="region" type="Rect2i" />
			<description>
				Returns [member terrain_data]'s heights covering [param region]; see [method TerrainData.get_height_region].
			</description>
		</method>
		<method name="get_hole_region" qualifiers="const">
			<return type="PackedByteArray" />
			<param index="0" name="region" type="Rect2i" />
			<description>
				Returns [member terrain_data]'s hole flags covering [param region]; see [method TerrainData.get_hole_region].
			</description>
		</method>
		<method name="get_layer_weight_region" qualifiers="const">
			<return type="PackedFloat32Array" />
			<param index="0" name="region" type="Rect2i" />
			<param index="1" name="layer_index" type="int" />
			<description>
				Returns [member terrain_data]'s weights for the [TerrainLayer] at [param layer_index] covering [param region]; see [method TerrainData.get_layer_weight_region].
			</description>
		</method>
		<method name="paint_layer">
			<return type="void" />
			<param index="0" name="local_position" type="Vector3" />
			<param index="1" name="radius" type="float" />
			<param index="2" name="strength" type="float" />
			<param index="3" name="layer_index" type="int" />
			<description>
				Raises the weight of the [TerrainLayer] at [param layer_index] in [member layers] within [param radius] meters of [param local_position] (in this node's local space; only the X and Z coordinates matter), by [param strength] per call (a value around [code]0.1[/code] to [code]1.0[/code] is typical), falling off smoothly towards the edge of the brush. Every other layer's weight at each affected point is lowered proportionally so the total stays roughly constant (see [TerrainData]'s description), so repeatedly painting one layer converges on it fully replacing whatever was there rather than just capping out at an even split with it. Re-uploads the affected layer weight textures, but does not need to rebuild any chunk mesh or collision shape, since painting never changes geometry.
			</description>
		</method>
		<method name="sculpt">
			<return type="void" />
			<param index="0" name="local_position" type="Vector3" />
			<param index="1" name="radius" type="float" />
			<param index="2" name="strength" type="float" />
			<param index="3" name="operation" type="int" enum="Landscape3D.SculptOperation" />
			<param index="4" name="flatten_height" type="float" default="0.0" />
			<param index="5" name="update_collision" type="bool" default="true" />
			<description>
				Sculpts [member terrain_data]'s heightmap within [param radius] meters of [param local_position] (in this node's local space; only the X and Z coordinates matter), falling off smoothly towards the edge of the brush. [param strength] is the maximum height change, in meters, applied at the center of the brush per call, except for [constant SCULPT_FLATTEN] where it is instead how far each height moves towards [param flatten_height] (as a 0-1 fraction; values at or above 1 snap immediately). Rebuilds the chunks the brush touches and, unless [param update_collision] is [code]false[/code], also updates the collision shape immediately; pass [code]false[/code] while repeatedly sculpting in a tight loop (e.g. dragging a brush) and call [method update_collision] once afterwards instead, since rebuilding the collision shape from the whole heightmap on every call is comparatively expensive.
			</description>
		</method>
		<method name="set_height_region">
			<return type="void" />
			<param index="0" name="region" type="Rect2i" />
			<param index="1" name="heights" type="PackedFloat32Array" />
			<param index="2" name="update_collision" type="bool" default="true" />
			<description>
				Writes [param heights] into [member terrain_data] at [param region] (see [method TerrainData.set_height_region]) and rebuilds the chunks [param region] overlaps. See [method sculpt] for [param update_collision].
			</description>
		</method>
		<method name="set_hole">
			<return type="void" />
			<param index="0" name="local_position" type="Vector3" />
			<param index="1" name="radius" type="float" />
			<param index="2" name="hole" type="bool" />
			<param index="3" name="update_collision" type="bool" default="true" />
			<description>
				Sets or clears the hole flag within [param radius] meters of [param local_position] (in this node's local space; only the X and Z coordinates matter) and rebuilds the chunks the brush touches. [b]Note:[/b] holes only affect the rendered geometry, not the collision shape (see the class description and [method TerrainData.is_hole]); [param update_collision] is accepted for consistency with [method sculpt] and [method set_height_region] but has no effect here.
			</description>
		</method>
		<method name="set_hole_region">
			<return type="void" />
			<param index="0" name="region" type="Rect2i" />
			<param index="1" name="holes" type="PackedByteArray" />
			<description>
				Writes [param holes] into [member terrain_data] at [param region] (see [method TerrainData.set_hole_region]) and rebuilds the chunks [param region] overlaps (holes affect chunk geometry; see [method TerrainData.is_hole]).
			</description>
		</method>
		<method name="set_layer_weight_region">
			<return type="void" />
			<param index="0" name="region" type="Rect2i" />
			<param index="1" name="layer_index" type="int" />
			<param index="2" name="weights" type="PackedFloat32Array" />
			<description>
				Writes [param weights] for the [TerrainLayer] at [param layer_index] into [member terrain_data] at [param region] (see [method TerrainData.set_layer_weight_region]) and re-uploads that layer's weight texture. Unlike [method paint_layer], this does not touch any other layer's weight, so it won't by itself keep the total weight at each point normalized (see [TerrainData]'s description) - prefer [method paint_layer] for brush-style painting.
			</description>
		</method>
		<method name="update_collision">
			<return type="void" />
			<description>
				Rebuilds the terrain's [HeightMapShape3D] from the current [member terrain_data] heights. Called automatically when [member terrain_data] changes and after each editor brush stroke; call it manually after scripted edits made with [param update_collision] set to [code]false[/code].
			</description>
		</method>
	</methods>
	<members>
		<member name="cast_shadow" type="int" setter="set_cast_shadow" getter="get_cast_shadow" enum="GeometryInstance3D.ShadowCastingSetting" default="1">
			The shadow casting mode applied to every chunk, equivalent to [member GeometryInstance3D.cast_shadow].
		</member>
		<member name="collision_layer" type="int" setter="set_collision_layer" getter="get_collision_layer" default="1">
			The terrain's physics collision layer, equivalent to [member CollisionObject3D.collision_layer].
		</member>
		<member name="collision_mask" type="int" setter="set_collision_mask" getter="get_collision_mask" default="1">
			The terrain's physics collision mask, equivalent to [member CollisionObject3D.collision_mask].
		</member>
		<member name="debug_draw_chunks" type="bool" setter="set_debug_draw_chunks" getter="is_debug_draw_chunks_enabled" default="false">
			If [code]true[/code], draws each chunk's bounds as a wireframe box in the editor while this node is selected, to help visualize the chunk grid described in the class description.
		</member>
		<member name="gi_mode" type="int" setter="set_gi_mode" getter="get_gi_mode" enum="GeometryInstance3D.GIMode" default="1">
			The global illumination mode applied to every chunk, equivalent to [member GeometryInstance3D.gi_mode]. Defaults to [constant GeometryInstance3D.GI_MODE_STATIC], unlike most small scattered geometry, since a terrain is usually a good candidate for baked lighting.
		</member>
		<member name="layers" type="TerrainLayer[]" setter="set_layers" getter="get_layers" default="[]">
			The ground materials this terrain can be painted with, in the order the Paint brush's layer picker lists them. See [TerrainLayer] and the class description; up to 32 entries are supported.
		</member>
		<member name="lod_bias" type="float" setter="set_lod_bias" getter="get_lod_bias" default="1.0">
			Scales how aggressively chunks switch to a lower level of detail with distance. Values above [code]1.0[/code] switch to a lower level of detail sooner (closer to the camera); values below [code]1.0[/code] delay switching, keeping more chunks at full detail for longer at the cost of performance.
		</member>
		<member name="occluder_detail" type="int" setter="set_occluder_detail" getter="get_occluder_detail" default="8">
			How finely the terrain's occluders follow its surface, as the number of quads along each edge of a chunk's occluder (always rounded down to a power of two, at most 32). Higher values follow hills and ridges more closely and therefore cull more, at the cost of more triangles to rasterize into the occlusion buffer every frame; lower values are cheaper but flatten the terrain out, which only ever culls less. Has no effect unless [member occluder_enabled] is [code]true[/code].
			Because an occluder may never claim to hide something the terrain does not actually hide, every occluder vertex is pushed down to the lowest heightmap sample within one occluder quad of it. A narrow ridge that is thinner than an occluder quad flattens away entirely at low detail, so terrains made of sharp, narrow features need a higher value here than smooth, rolling ones to occlude as much.
		</member>
		<member name="occluder_enabled" type="bool" setter="set_occluder_enabled" getter="is_occluder_enabled" default="true">
			If [code]true[/code], the terrain feeds the renderer's occlusion culling a simplified copy of its surface (see [member occluder_detail]), so that objects standing behind hills are culled before they are drawn - vegetation especially, which is usually the densest thing a terrain carries.
			This only does anything while occlusion culling is on ([member ProjectSettings.rendering/occlusion_culling/use_occlusion_culling], or [member Viewport.use_occlusion_culling] for a custom viewport); until then it costs nothing but the memory of the simplified surface. Holes in the terrain (see [method set_hole]) never occlude, since they can be seen through.
			[b]Note:[/b] Individual objects can opt out of being culled this way with [member GeometryInstance3D.ignore_occlusion_culling], or [member FoliageSpawner3D.cell_ignore_occlusion_culling] for a whole spawner's foliage.
		</member>
		<member name="pom_enabled" type="bool" setter="set_pom_enabled" getter="is_pom_enabled" default="false">
			Master switch for Parallax Occlusion Mapping (see the class description): a [TerrainLayer] also needs its own [member TerrainLayer.pom_enabled] set for POM to apply to it. Off by default: the ray-marching this adds to every fragment covered by a layer that has it isn't free.
		</member>
		<member name="pom_fade_end" type="float" setter="set_pom_fade_end" getter="get_pom_fade_end" default="60.0">
			The distance, in meters from the camera, at which [member pom_enabled]'s depth reaches fully flat; see [member pom_fade_start].
		</member>
		<member name="pom_fade_start" type="float" setter="set_pom_fade_start" getter="get_pom_fade_start" default="20.0">
			The distance, in meters from the camera, at which [member pom_enabled]'s apparent depth starts fading towards flat, reaching zero at [member pom_fade_end]. Keeps distant, minified chunks from shimmering/aliasing under parallax; set [member pom_fade_end] to a value at or below this to disable the fade and use full depth at any distance.
		</member>
		<member name="pom_flip_binormal" type="bool" setter="set_pom_flip_binormal" getter="get_pom_flip_binormal" default="false">
			Flips the effective binormal direction used to offset [member pom_enabled]'s parallax effect. Toggle this (and/or [member pom_flip_tangent]) if the parallax effect looks inverted (bulging instead of recessed, or shifted the wrong way as the camera moves) - equivalent to [member BaseMaterial3D.heightmap_flip_binormal].
		</member>
		<member name="pom_flip_tangent" type="bool" setter="set_pom_flip_tangent" getter="get_pom_flip_tangent" default="false">
			Flips the effective tangent direction used to offset [member pom_enabled]'s parallax effect. See [member pom_flip_binormal]; equivalent to [member BaseMaterial3D.heightmap_flip_tangent].
		</member>
		<member name="pom_max_layers" type="int" setter="set_pom_max_layers" getter="get_pom_max_layers" default="32">
			The number of ray-marching steps [member pom_enabled] uses when looking nearly straight down at the surface. Equivalent to [member BaseMaterial3D.heightmap_deep_parallax_max_layers].
		</member>
		<member name="pom_min_layers" type="int" setter="set_pom_min_layers" getter="get_pom_min_layers" default="8">
			The number of ray-marching steps [member pom_enabled] uses when looking nearly edge-on at the surface (fewer steps are needed at a glancing angle without a visible quality loss, so this saves performance over always using [member pom_max_layers] steps). Equivalent to [member BaseMaterial3D.heightmap_deep_parallax_min_layers].
		</member>
		<member name="pom_self_shadow_enabled" type="bool" setter="set_pom_self_shadow_enabled" getter="is_pom_self_shadow_enabled" default="true">
			If [code]true[/code] (and [member pom_enabled] is also [code]true[/code]), darkens points on a POM-enabled layer whose [member TerrainLayer.height_texture] detail would shadow them from [member pom_shadow_light_direction]. This is an approximation computed entirely from each layer's own height texture (see the class description), not a real shadow from any light in the scene, and only ever applies to a layer that already has [member TerrainLayer.pom_enabled] set.
		</member>
		<member name="pom_shadow_light_direction" type="Vector3" setter="set_pom_shadow_light_direction" getter="get_pom_shadow_light_direction" default="Vector3(0.5, 0.75, 0.3)">
			The world-space direction, from a surface point towards the light, that [member pom_self_shadow_enabled] marches towards to find shadowing detail. Point this at whatever your main light's direction is; it does not update automatically if that light moves or rotates.
		</member>
		<member name="pom_shadow_steps" type="int" setter="set_pom_shadow_steps" getter="get_pom_shadow_steps" default="8">
			The number of ray-marching steps [member pom_self_shadow_enabled] uses. Higher values give smoother, more accurate self-shadows at a higher cost.
		</member>
		<member name="pom_shadow_strength" type="float" setter="set_pom_shadow_strength" getter="get_pom_shadow_strength" default="1.0">
			Scales how dark [member pom_self_shadow_enabled]'s shadows can get; [code]0[/code] disables their visible effect (without paying much less than [member pom_self_shadow_enabled] [code]= false[/code] would, since the march itself still runs), [code]1[/code] allows a fully dark shadow.
		</member>
		<member name="skirt_depth" type="float" setter="set_skirt_depth" getter="get_skirt_depth" default="2.0">
			How far down, in meters, the hidden "skirt" geometry around each chunk's border extends (see the class description). It should be large enough to cover the biggest height difference likely to appear between two neighboring chunks at different levels of detail; the default is generous for most terrains.
		</member>
		<member name="terrain_data" type="TerrainData" setter="set_terrain_data" getter="get_terrain_data">
			The [TerrainData] this node renders, sculpts, and collides with. A new, empty (flat) [TerrainData] is often the fastest way to start; assign one and use the editor brush to sculpt it, or [method TerrainData.import_heightmap] to start from an existing heightmap image.
		</member>
	</members>
	<constants>
		<constant name="SCULPT_RAISE" value="0" enum="SculptOperation">
			Raises the height within the brush.
		</constant>
		<constant name="SCULPT_LOWER" value="1" enum="SculptOperation">
			Lowers the height within the brush.
		</constant>
		<constant name="SCULPT_SMOOTH" value="2" enum="SculptOperation">
			Averages the height within the brush with its immediate neighbors, smoothing out sharp detail.
		</constant>
		<constant name="SCULPT_FLATTEN" value="3" enum="SculptOperation">
			Moves the height within the brush towards a fixed target height (see [method sculpt]'s [code]flatten_height[/code] parameter), flattening it.
		</constant>
	</constants>
</class>
