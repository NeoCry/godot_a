<?xml version="1.0" encoding="UTF-8" ?>
<class name="VoxelGI" inherits="VisualInstance3D" api_type="core" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="../class.xsd">
	<brief_description>
		Real-time global illumination (GI) probe.
	</brief_description>
	<description>
		[VoxelGI]s are used to provide high-quality real-time indirect light and reflections to scenes. They precompute the effect of objects that emit light and the effect of static geometry to simulate the behavior of complex light in real-time. [VoxelGI]s need to be baked before having a visible effect. However, once baked, dynamic objects will receive light from them. Furthermore, lights can be fully dynamic or baked.
		[b]Note:[/b] [VoxelGI] is only supported in the Forward+ rendering method, not Mobile or Compatibility.
		[b]Procedural generation:[/b] [VoxelGI] can be baked in an exported project, which makes it suitable for procedurally generated or user-built levels as long as all the geometry is generated in advance. For games where geometry is generated at any time during gameplay, SDFGI is more suitable (see [member Environment.sdfgi_enabled]).
		[b]Performance:[/b] [VoxelGI] is relatively demanding on the GPU and is not suited to low-end hardware such as integrated graphics (consider [LightmapGI] instead). To improve performance, adjust [member ProjectSettings.rendering/global_illumination/voxel_gi/quality] and enable [member ProjectSettings.rendering/global_illumination/gi/use_half_resolution] in the Project Settings. To provide a fallback for low-end hardware, consider adding an option to disable [VoxelGI] in your project's options menus. A [VoxelGI] node can be disabled by hiding it.
		[b]Note:[/b] Meshes should have sufficiently thick walls to avoid light leaks (avoid one-sided walls). For interior levels, enclose your level geometry in a sufficiently large box and bridge the loops to close the mesh. To further prevent light leaks, you can also strategically place temporary [MeshInstance3D] nodes with their [member GeometryInstance3D.gi_mode] set to [constant GeometryInstance3D.GI_MODE_STATIC]. These temporary nodes can then be hidden after baking the [VoxelGI] node.
	</description>
	<tutorials>
		<link title="Using Voxel global illumination">$DOCS_URL/tutorials/3d/global_illumination/using_voxel_gi.html</link>
		<link title="Third Person Shooter (TPS) Demo">https://godotengine.org/asset-library/asset/2710</link>
	</tutorials>
	<methods>
		<method name="bake">
			<return type="void" />
			<param index="0" name="from_node" type="Node" default="null" />
			<param index="1" name="create_visual_debug" type="bool" default="false" />
			<description>
				Bakes the effect from all [GeometryInstance3D]s marked with [constant GeometryInstance3D.GI_MODE_STATIC] and [Light3D]s marked with either [constant Light3D.BAKE_STATIC] or [constant Light3D.BAKE_DYNAMIC]. If [param create_visual_debug] is [code]true[/code], after baking the light, this will generate a [MultiMesh] that has a cube representing each solid cell with each cube colored to the cell's albedo color. This can be used to visualize the [VoxelGI]'s data and debug any issues that may be occurring.
				[b]Note:[/b] [method bake] works from the editor and in exported projects. This makes it suitable for procedurally generated or user-built levels. Baking a [VoxelGI] node generally takes from 5 to 20 seconds in most scenes. Raising [member voxel_size] can speed up baking.
				[b]Note:[/b] [GeometryInstance3D]s and [Light3D]s must be fully ready before [method bake] is called. If you are procedurally creating those and some meshes or lights are missing from your baked [VoxelGI], use [code]call_deferred("bake")[/code] instead of calling [method bake] directly.
			</description>
		</method>
		<method name="debug_bake">
			<return type="void" />
			<description>
				Calls [method bake] with [code]create_visual_debug[/code] enabled.
			</description>
		</method>
		<method name="get_effective_voxel_size" qualifiers="const">
			<return type="float" />
			<description>
				Returns the voxel edge length, in world units, that [method bake] will actually use for the current [member size] and [member voxel_size]. This is at most [member voxel_size], since the cell count is rounded up to a power of two, and larger than it only when the octree depth had to be clamped to [constant MAX_OCTREE_DEPTH].
			</description>
		</method>
		<method name="get_estimated_video_memory" qualifiers="const">
			<return type="int" />
			<description>
				Returns how much video memory, in bytes, the probe will occupy while rendering with the current [member size], [member voxel_size] and [member data]. This counts the full mip chain the runtime allocates over the voxel grid, and the 6 extra chains that [member VoxelGIData.anisotropic_strength] adds when it is above [code]0.0[/code]. It does not include the baked octree itself, whose size depends on the geometry and is only known after baking.
			</description>
		</method>
		<method name="get_octree_depth" qualifiers="const">
			<return type="int" />
			<description>
				Returns the octree depth [method bake] will use for the current [member size] and [member voxel_size], clamped to [constant MIN_OCTREE_DEPTH]&#8230;[constant MAX_OCTREE_DEPTH]. The probe's longest axis gets [code]1 &lt;&lt; depth[/code] voxels.
			</description>
		</method>
	</methods>
	<members>
		<member name="camera_attributes" type="CameraAttributes" setter="set_camera_attributes" getter="get_camera_attributes">
			The [CameraAttributes] resource that specifies exposure levels to bake at. Auto-exposure and non exposure properties will be ignored. Exposure settings should be used to reduce the dynamic range present when baking. If exposure is too high, the [VoxelGI] will have banding artifacts or may have over-exposure artifacts.
		</member>
		<member name="data" type="VoxelGIData" setter="set_probe_data" getter="get_probe_data">
			The [VoxelGIData] resource that holds the data for this [VoxelGI].
		</member>
		<member name="size" type="Vector3" setter="set_size" getter="get_size" default="Vector3(20, 20, 20)">
			The size of the area covered by the [VoxelGI]. This must be [code]1.0[/code] or greater on each axis.
			Voxel detail is set in world units by [member voxel_size] rather than as a subdivision count, so resizing the probe keeps the lighting detail and changes the number of voxels instead. Growing the probe past what [constant MAX_OCTREE_DEPTH] can address is the one case where the voxels do get coarser; the node reports a configuration warning when that happens.
		</member>
		<member name="voxel_size" type="float" setter="set_voxel_size" getter="get_voxel_size" default="0.25">
			Target edge length of one voxel, in world units (metres). Smaller values give finer indirect lighting at a steep cost: halving this multiplies both the baked octree and the runtime VRAM of the probe by up to 8.
			Indirect light is low frequency, so values in the [code]0.25[/code]&#8211;[code]1.0[/code] range are usually enough even for large interiors; sub-[code]0.1[/code] voxels are rarely worth their memory.
			The bake rounds the cell count up to a power of two, so the voxels it actually uses are never coarser than this value. Use [method get_effective_voxel_size] to read the size that will really be baked, and [method get_octree_depth] for the resulting octree depth.
		</member>
	</members>
	<constants>
		<constant name="MAX_OCTREE_DEPTH" value="10">
			Deepest octree the baked cell format can address, giving at most [code]1024[/code] voxels along the probe's longest axis. A baked cell packs its position into one 32-bit word as 11/10/11 bits, and the 10-bit field sets this limit.
		</constant>
		<constant name="MIN_OCTREE_DEPTH" value="1">
			Shallowest octree the bake will produce, giving [code]2[/code] voxels along the probe's longest axis.
		</constant>
	</constants>
</class>
